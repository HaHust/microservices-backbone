# Name service
spring:
  application:
    name: service-one

  # We already start configuring config-client
  profiles:
    active: dev
  config:
    import: optional:configserver:http://localhost:8888