

server:
  port: 8888

spring:
  application:
    name: config-server

  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/HaHust/microservices-config
          clone-on-start: true
          default-label: master





